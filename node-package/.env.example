# Заполняется автоматически скриптом install.sh — править вручную обычно не нужно.
# Токен авторизации node-api (должен совпадать с тем, что введён в админке основного сервера).
XRAY_NODE_TOKEN=
# Порт, на котором node-api слушает запросы основного сервера.
NODE_API_PORT=9090
